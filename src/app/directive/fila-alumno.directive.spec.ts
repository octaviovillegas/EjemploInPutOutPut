import { FilaAlumnoDirective } from './fila-alumno.directive';

describe('FilaAlumnoDirective', () => {
  it('should create an instance', () => {
    const directive = new FilaAlumnoDirective();
    expect(directive).toBeTruthy();
  });
});
